using UnityEngine;

namespace MainScene
{
    public class MainScene : MonoBehaviour
    {
        [SerializeField]
        GameObject m_BallPrf;


        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

    } // class

} // namespace